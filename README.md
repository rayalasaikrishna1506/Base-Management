# Base-Management
Base Master Management System built with ASP.NET Core Web API, React, and Microsoft SQL Server Express. Implements full CRUD operations using a clean layered architecture (Controller, Service, DTO) with EF Core, async programming, and a responsive UI for managing Base records.
